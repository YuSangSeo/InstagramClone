# Migration `20201005174354-init`

This migration has been generated by YuSangSeo at 10/6/2020, 2:43:54 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `Post` (
`id` varchar(191)  NOT NULL ,
`userId` varchar(191)  NOT NULL ,
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`title` varchar(191)  NOT NULL ,
`content` varchar(191)  NOT NULL ,
`views` int  NOT NULL DEFAULT 0,
`todayViews` int  NOT NULL DEFAULT 0,
INDEX `userId`(`userId`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `User` (
`id` varchar(191)  NOT NULL ,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`avatar` varchar(191)  DEFAULT 'http://s3.amazonaws.com/redqteam.com/isomorphic-reloaded-image/profilepic.png',
`password` varchar(191)  NOT NULL ,
`name` varchar(191)  NOT NULL ,
`phone` varchar(191)  NOT NULL ,
`permit` boolean  NOT NULL DEFAULT true,
`auth` int  NOT NULL DEFAULT 0,
UNIQUE INDEX `User.phone_unique`(`phone`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `Market` (
`id` varchar(191)  NOT NULL ,
`userId` varchar(191)  NOT NULL ,
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`title` varchar(191)  NOT NULL ,
`content` varchar(191)  NOT NULL ,
`views` int  NOT NULL DEFAULT 0,
`todayViews` int  NOT NULL DEFAULT 0,
INDEX `userId`(`userId`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `Shop` (
`id` varchar(191)  NOT NULL ,
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`title` varchar(191)  NOT NULL ,
`content` varchar(191)  NOT NULL ,
`views` int  NOT NULL DEFAULT 0,
`todayViews` int  NOT NULL DEFAULT 0,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `marketComment` (
`id` varchar(191)  NOT NULL ,
`userId` varchar(191)  NOT NULL ,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`text` varchar(191)  NOT NULL ,
`marketId` varchar(191)  NOT NULL ,
INDEX `marketId`(`marketId`),
INDEX `userId`(`userId`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `marketImage` (
`id` varchar(191)  NOT NULL ,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`url` varchar(191)  NOT NULL ,
`marketId` varchar(191)  ,
INDEX `marketId`(`marketId`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `postComment` (
`id` varchar(191)  NOT NULL ,
`userId` varchar(191)  NOT NULL ,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`text` varchar(191)  NOT NULL ,
`postId` varchar(191)  NOT NULL ,
INDEX `postId`(`postId`),
INDEX `userId`(`userId`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `postImage` (
`id` varchar(191)  NOT NULL ,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`url` varchar(191)  NOT NULL ,
`postId` varchar(191)  NOT NULL ,
INDEX `postId`(`postId`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `shopComment` (
`id` varchar(191)  NOT NULL ,
`userId` varchar(191)  NOT NULL ,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`text` varchar(191)  NOT NULL ,
`shopId` varchar(191)  NOT NULL ,
INDEX `shopId`(`shopId`),
INDEX `userId`(`userId`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `shopImage` (
`id` varchar(191)  NOT NULL ,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`url` varchar(191)  NOT NULL ,
`shopId` varchar(191)  NOT NULL ,
INDEX `shopId`(`shopId`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `Post` ADD FOREIGN KEY (`userId`) REFERENCES `PrismaPractice`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `Market` ADD FOREIGN KEY (`userId`) REFERENCES `PrismaPractice`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `marketComment` ADD FOREIGN KEY (`marketId`) REFERENCES `PrismaPractice`.`Market`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `marketComment` ADD FOREIGN KEY (`userId`) REFERENCES `PrismaPractice`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `marketImage` ADD FOREIGN KEY (`marketId`) REFERENCES `PrismaPractice`.`Market`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE `postComment` ADD FOREIGN KEY (`postId`) REFERENCES `PrismaPractice`.`Post`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `postComment` ADD FOREIGN KEY (`userId`) REFERENCES `PrismaPractice`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `postImage` ADD FOREIGN KEY (`postId`) REFERENCES `PrismaPractice`.`Post`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `shopComment` ADD FOREIGN KEY (`shopId`) REFERENCES `PrismaPractice`.`Shop`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `shopComment` ADD FOREIGN KEY (`userId`) REFERENCES `PrismaPractice`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `shopImage` ADD FOREIGN KEY (`shopId`) REFERENCES `PrismaPractice`.`Shop`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201005174354-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,144 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "mysql"
+  url = "***"
+}
+
+model Post {
+  id          String        @id
+  userId      String
+  updatedAt   DateTime      @default(now())
+  createdAt   DateTime      @default(now())
+  title       String
+  content     String
+  views       Int           @default(0)
+  todayViews  Int           @default(0)
+  User        User          @relation(fields: [userId], references: [id])
+  postComment postComment[]
+  postImage   postImage[]
+
+  @@index([userId], name: "userId")
+}
+
+model User {
+  id            String          @id
+  createdAt     DateTime        @default(now())
+  updatedAt     DateTime        @default(now())
+  avatar        String?         @default("http://s3.amazonaws.com/redqteam.com/isomorphic-reloaded-image/profilepic.png")
+  password      String
+  name          String
+  phone         String          @unique
+  permit        Boolean         @default(true)
+  auth          Int             @default(0)
+  Market        Market[]
+  Post          Post[]
+  marketComment marketComment[]
+  postComment   postComment[]
+  shopComment   shopComment[]
+}
+
+model Market {
+  id            String          @id
+  userId        String
+  updatedAt     DateTime        @default(now())
+  createdAt     DateTime        @default(now())
+  title         String
+  content       String
+  views         Int             @default(0)
+  todayViews    Int             @default(0)
+  User          User            @relation(fields: [userId], references: [id])
+  marketComment marketComment[]
+  marketImage   marketImage[]
+
+  @@index([userId], name: "userId")
+}
+
+model Shop {
+  id          String        @id
+  updatedAt   DateTime      @default(now())
+  createdAt   DateTime      @default(now())
+  title       String
+  content     String
+  views       Int           @default(0)
+  todayViews  Int           @default(0)
+  shopComment shopComment[]
+  shopImage   shopImage[]
+}
+
+model marketComment {
+  id        String   @id
+  userId    String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  text      String
+  marketId  String
+  Market    Market   @relation(fields: [marketId], references: [id])
+  User      User     @relation(fields: [userId], references: [id])
+
+  @@index([marketId], name: "marketId")
+  @@index([userId], name: "userId")
+}
+
+model marketImage {
+  id        String   @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  url       String
+  marketId  String?
+  Market    Market?  @relation(fields: [marketId], references: [id])
+
+  @@index([marketId], name: "marketId")
+}
+
+model postComment {
+  id        String   @id
+  userId    String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  text      String
+  postId    String
+  Post      Post     @relation(fields: [postId], references: [id])
+  User      User     @relation(fields: [userId], references: [id])
+
+  @@index([postId], name: "postId")
+  @@index([userId], name: "userId")
+}
+
+model postImage {
+  id        String   @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  url       String
+  postId    String
+  Post      Post     @relation(fields: [postId], references: [id])
+
+  @@index([postId], name: "postId")
+}
+
+model shopComment {
+  id        String   @id
+  userId    String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  text      String
+  shopId    String
+  Shop      Shop     @relation(fields: [shopId], references: [id])
+  User      User     @relation(fields: [userId], references: [id])
+
+  @@index([shopId], name: "shopId")
+  @@index([userId], name: "userId")
+}
+
+model shopImage {
+  id        String   @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  url       String
+  shopId    String
+  Shop      Shop     @relation(fields: [shopId], references: [id])
+
+  @@index([shopId], name: "shopId")
+}
```


